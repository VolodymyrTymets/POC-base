# Optional. Only needed to run a second stack alongside the default one —
# see docs/RUNBOOK.md's "Running two (or more) stacks side by side" and
# docs/decisions/ADR-0009-configurable-ports.md. Generate with ./set-ports.sh
# <BASE_PORT> rather than editing these by hand.
API_PORT=3001
POSTGRES_PORT=5432
REDIS_PORT=6379
WEB_APP_PORT=5173
WEB_ADMIN_PORT=5174
DATABASE_NAME=POC
